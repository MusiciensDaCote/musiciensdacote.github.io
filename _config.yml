# remote_theme: zendesk/jekyll-theme-zendesk-garden@main
remote_theme: pages-themes/slate@main
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

title: Les musiciens d'à côté
lsi: false
safe: true
incremental: false
highlighter: rouge
gist:
  noscript: false
kramdown:
  math_engine: mathjax
  syntax_highlighter: rouge

# Ceci marche avec le thème some code in between three mais ça prend trop de place.
sidebar:
  - doodles
  - admin
